export interface SelectedSearchResultsData {
    CustomMessages: CustomMessages;
}

interface CustomMessages {
    Address: string;
    Postcode: string;
    FloorArea: string;
    NumberOfRooms: string;
    SelectedProperties: string;
    ListOfSelectedProperties: string;
}